name: VS-73 live Instagram production smoke
on:
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  smoke:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Disabled while fixing real-data Research grounding
        run: echo "Live Instagram publish disabled"
