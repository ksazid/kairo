name: VS-73 live Instagram production smoke
on:
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  smoke:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Disabled after provider-confirmed real-data publish
        run: echo "Live Instagram publish smoke completed and disabled"
