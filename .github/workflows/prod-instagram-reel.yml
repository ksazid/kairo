name: VS-73 one-shot Instagram Reel publish
on:
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  publish-reel:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while full Reel media is reconstructed and verified."
