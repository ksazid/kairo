# Hunter Chunks 1–4 Integration Certification

Scope: certify the integrated lifecycle before Chunk 5.

Test path:

`Ready Brand → Brand Intelligence Snapshot → Discovery Plan → Hunter source planning/evidence → deterministic quality boundary → opportunity persistence → Hunter Run record → Brand Brain activation`

Certification matrix:
- Kairo / AI technology: successful evidence-backed opportunity.
- Harbour Kitchen / hospitality: successful opportunity while YouTube is degraded; remaining providers continue.
- Noorpath / Umrah: truthful zero-result path; no model invocation and no filler opportunity.

Required assertions:
- exact `snapshotVersion` and `planVersion` lineage.
- Discovery Plan topics/exclusions reach Hunter.
- evidence is fetched through the normalized source boundary.
- only quality-qualified opportunities are persisted.
- zero evidence produces zero candidates/opportunities.
- degraded providers are surfaced without failing the whole run.
- one immutable run record per physical Hunter run.
- Brand Brain activation exposes the latest real run and keeps schedule `null` before Chunk 5.
- tenant isolation holds for Hunter run history.

Executable certification: `apps/api/src/hunter-chunk1-4-integration.test.ts`.
