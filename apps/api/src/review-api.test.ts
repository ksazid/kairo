import {describe,expect,it}from"vitest";import type{ExternalIdentity}from"@kairo/contracts";import type{IdentityVerifier}from"./auth";import{buildApp}from"./app";import{MemoryKairoRepository}from"./store";import{MemoryResearchRepository}from"./research-store";import{MemoryCampaignRepository}from"./campaign-store";import{MemoryReviewRepository}from"./review-store";
class Verifier implements IdentityVerifier{async verify(v:string|undefined):Promise<ExternalIdentity|null>{return v?.startsWith("Bearer test:")?{provider:"test",subject:v.slice(12)}:null}}
describe("VS-06 Review API",()=>{it("reviews and binds human approval to the current version",async()=>{const store=new MemoryKairoRepository(),research=new MemoryResearchRepository(store),campaigns=new MemoryCampaignRepository(store),reviews=new MemoryReviewRepository(store);const app=buildApp({store,researchStore:research,campaignStore:campaigns,reviewStore:reviews,criticEvaluator:{async evaluate(){return{passed:true,score:90,findings:[]}}},identityVerifier:new Verifier()});const headers={authorization:"Bearer test:alice"};const setup=await app.inject({method:"POST",url:"/api/v1/workspaces",headers,payload:{workspaceName:"Studio",brandName:"Kairo"}}),brand=setup.json().brand.id;const idea=await app.inject({method:"POST",url:`/api/v1/brands/${brand}/ideas`,headers,payload:{title:"Evidence",premise:"Explain"}});await research.seedReadyBundle(idea.json().id);await app.inject({method:"POST",url:`/api/v1/brands/${brand}/ideas/${idea.json().id}/angles/angle-1/select`,headers,payload:{expectedVersion:1}});const campaign=await app.inject({method:"POST",url:`/api/v1/brands/${brand}/campaigns`,headers,payload:{ideaId:idea.json().id,name:"Campaign",objective:"Educate"}});const made=await app.inject({method:"POST",url:`/api/v1/brands/${brand}/campaigns/${campaign.json().id}/assets`,headers,payload:{channel:"linkedin",format:"text",audience:"Founders",topic:"Evidence",hookType:"data",cta:"Read",content:"Supported"}}),asset=made.json().assets[0].asset;const reviewed=await app.inject({method:"POST",url:`/api/v1/brands/${brand}/campaigns/${campaign.json().id}/assets/${asset.id}/review`,headers,payload:{expectedVersion:1,brandContextVersion:`${brand}@1`,revisionCycle:0}});expect(reviewed.statusCode).toBe(201);expect(reviewed.json().status).toBe("passed");const approved=await app.inject({method:"POST",url:`/api/v1/brands/${brand}/campaigns/${campaign.json().id}/assets/${asset.id}/approve`,headers,payload:{expectedVersion:1,destination:{channel:"linkedin",accountRef:"company-page"}}});expect(approved.statusCode).toBe(201);expect(approved.json()).toMatchObject({version:1,destination:{channel:"linkedin"}});await app.close()})});
