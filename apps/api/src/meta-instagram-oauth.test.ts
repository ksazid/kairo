import{describe,expect,it}from"vitest";import{MetaInstagramOAuthClient,metaInstagramRequestedScopes}from"./meta-instagram-oauth";
describe("VS-17 Meta Instagram OAuth adapter",()=>{it("keeps app secret out of the browser authorization URL and discovers only linked Professional accounts",async()=>{const calls:Array<{url:string;init?:RequestInit}>=[];const client=new MetaInstagramOAuthClient("app-1","top-secret","v99.0","https://kairo.example/channels/instagram/callback",async(input,init)=>{const url=String(input);calls.push({url,init});if(url.endsWith("/oauth/access_token"))return new Response(JSON.stringify({access_token:"user-token"}),{status:200});if(url.endsWith("/me/permissions"))return new Response(JSON.stringify({data:metaInstagramRequestedScopes().map(permission=>({permission,status:"granted"}))}),{status:200});if(url.includes("/me/accounts"))return new Response(JSON.stringify({data:[{id:"page-1",name:"Page One",access_token:"page-token",instagram_business_account:{id:"111"}},{id:"page-2",name:"No Instagram",access_token:"unused"}]}),{status:200});if(url.includes("/111?"))return new Response(JSON.stringify({id:"111",username:"brandone",name:"Brand One"}),{status:200});throw new Error(`unexpected ${url}`)});const authorize=client.authorizationUrl("state-value");expect(authorize).toContain("client_id=app-1");expect(authorize).not.toContain("top-secret");expect(authorize).not.toContain("user-token");const result=await client.exchangeAndDiscover("auth-code");expect(result.accounts).toEqual([{pageRef:"page-1",pageName:"Page One",accountRef:"111",displayName:"Brand One",username:"brandone",pageAccessToken:"page-token"}]);expect(calls.every(call=>!call.url.includes("top-secret")&&!call.url.includes("page-token")&&!call.url.includes("user-token"))).toBe(true);const exchange=calls.find(call=>call.url.endsWith("/oauth/access_token"))!;expect(String(exchange.init?.body)).toContain("client_secret=top-secret");expect(calls.filter(call=>call.url.includes("/me/")).every(call=>new Headers(call.init?.headers).get("authorization")==="Bearer user-token")).toBe(true)})});
