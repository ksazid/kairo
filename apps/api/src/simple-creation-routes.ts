import type{FastifyInstance,FastifyReply,FastifyRequest}from"fastify";import{KairoService,type KairoRepository}from"@kairo/domain";import type{IdentityVerifier}from"./auth";import{SimpleCreationService,type StartSimpleCreationInput}from"./simple-creation";
export function registerSimpleCreationRoutes(app:FastifyInstance,d:{coreStore:KairoRepository;identityVerifier:IdentityVerifier;service:SimpleCreationService;trigger?:()=>void}){const core=new KairoService(d.coreStore);app.post<{Params:{brandId:string};Body:StartSimpleCreationInput}>("/api/v1/brands/:brandId/simple-creations",async(q,r)=>{const a=await auth(q,r,core,d.identityVerifier);if(!a)return;const b=await core.getBrand(a.id,q.params.brandId);const value=await d.service.start(a.id,b.workspaceId,b.id,q.body);d.trigger?.();return r.status(202).send(await d.service.get(a.id,b.id,value.id));});app.get<{Params:{brandId:string;creationId:string}}>("/api/v1/brands/:brandId/simple-creations/:creationId",async(q,r)=>{const a=await auth(q,r,core,d.identityVerifier);if(!a)return;return d.service.get(a.id,q.params.brandId,q.params.creationId);});}
async function auth(q:FastifyRequest,r:FastifyReply,s:KairoService,v:IdentityVerifier){const i=await v.verify(q.headers.authorization);if(!i){await r.status(401).send({title:"Unauthorized",status:401,code:"unauthorized",correlationId:q.id});return null;}return s.establishSession(i);}
