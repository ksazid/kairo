import type{Pool}from"pg";import{ResourceNotFoundError}from"@kairo/domain";import type{CostEvent,OperationalFailure}from"@kairo/domain/operations";import type{OperationsTelemetrySink}from"./operations-runtime";import type{OperationsStore}from"./operations-store";
export class PgOperationsTelemetrySink implements OperationsTelemetrySink{constructor(private readonly pool:Pool,private readonly operations:OperationsStore){}async recordFailure(failure:OperationalFailure){await this.operations.recordFailure(failure)}async recordCost(event:CostEvent){const result=await this.pool.query(`insert into workflow_cost_events(id,workspace_id,brand_id,workflow_id,kind,provider,currency,cost_micros,occurred_at) select $1,$2,$3,$4,$5,$6,$7,$8,$9 from brands where id=$3 and workspace_id=$2 on conflict(id) do nothing returning id`,[event.id,event.workspaceId,event.brandId,event.workflowId,event.kind,event.provider,event.currency,event.costMicros,event.occurredAt]);if(!result.rowCount)throw new ResourceNotFoundError("Brand not found")}}
