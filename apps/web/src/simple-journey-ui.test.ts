import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=(p:string)=>readFileSync(new URL(p,import.meta.url),"utf8");describe("VS-79 default simple journey",()=>{it("starts Home with a goal and one strong create action",()=>{const home=source("../app/page.tsx");for(const text of ["What do you want to achieve?","Start with the outcome","Create content","Grow my audience","Build authority","Generate leads","Promote an offer"])expect(home).toContain(text)});it("preserves advanced research, evidence and results access",()=>{const home=source("../app/page.tsx");expect(home).toContain("Advanced details");expect(home).toContain("Research, Claims and evidence remain available");expect(home).toContain("Explore research-backed Opportunities")});it("provides an accessible sticky next step",()=>{const bar=source("../app/next-step-bar.tsx"),css=source("../app/discovery.css");expect(bar).toContain('aria-label="Recommended next step"');expect(bar).toContain("Turn one clear goal into content");expect(css).toContain("position: sticky")})});
