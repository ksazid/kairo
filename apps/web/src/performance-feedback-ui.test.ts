import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const source=readFileSync(new URL("../app/performance-feedback.tsx",import.meta.url),"utf8");describe("VS-78 recommendation feedback UI",()=>{it("explains freshness, accepted Learning influence and human authority",()=>{for(const text of ["Instagram metric status","accepted Learning","You remain in control","never replace the owner goal","automatically change Brand Brain","approve publishing"])expect(source).toContain(text)});it("links back to Brand-scoped Performance",()=>{expect(source).toContain("encodeURIComponent(brandId)");expect(source).toContain("Inspect Performance")})});
