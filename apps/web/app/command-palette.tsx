"use client";
import Link from "next/link";
import {useEffect,useMemo,useRef,useState} from "react";
import {KairoIcon,type KairoIconName} from "./kairo-icons";
type Item={label:string;detail:string;href:string;icon:KairoIconName};
export function CommandPalette({items}:{items:Item[]}){const [open,setOpen]=useState(false),[query,setQuery]=useState("");const dialog=useRef<HTMLDialogElement>(null);useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setOpen(value=>!value)}};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[]);useEffect(()=>{if(open&&!dialog.current?.open)dialog.current?.showModal();if(!open&&dialog.current?.open)dialog.current.close()},[open]);const filtered=useMemo(()=>items.filter(item=>`${item.label} ${item.detail}`.toLowerCase().includes(query.toLowerCase())).slice(0,12),[items,query]);return <><button className="command-trigger" type="button" onClick={()=>setOpen(true)}><KairoIcon name="search"/><span>Search and navigate</span><kbd>⌘K</kbd></button><dialog ref={dialog} className="command-dialog" onClose={()=>setOpen(false)} onClick={event=>{if(event.target===dialog.current)setOpen(false)}} aria-label="Search and navigate"><div className="command-box"><label><KairoIcon name="search"/><span className="sr-only">Search navigation</span><input autoFocus value={query} onChange={event=>setQuery(event.target.value)} placeholder="Find a page or Brand…"/></label><div className="command-results">{filtered.length?filtered.map(item=><Link key={`${item.label}-${item.href}`} href={item.href} onClick={()=>setOpen(false)}><KairoIcon name={item.icon}/><span><strong>{item.label}</strong><small>{item.detail}</small></span><KairoIcon name="chevron"/></Link>):<p>No matching page or Brand.</p>}</div><footer><span>Navigate with keyboard or pointer</span><button type="button" onClick={()=>setOpen(false)}>Close</button></footer></div></dialog></>}
