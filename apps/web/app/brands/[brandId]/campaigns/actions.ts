"use server";
import{redirect}from"next/navigation";import{appendContentEdit,approveContentVersionRequest,createCampaign,createContentAsset,generateContentVersion,getChannelAccounts,requestContentReview,scheduleApprovedContent}from"../../../../src/lib/kairo-api";import{publishApprovedContentNow}from"../../../../src/lib/publishing-api";import{saveContentVersionProductionAssets}from"../../../../src/lib/content-asset-library-api";import{distributeCampaignRequest,getChannelAccountGroups}from"../../../../src/lib/channel-account-groups-api";
const url=(b:string,c?:string)=>`/brands/${encodeURIComponent(b)}/campaigns${c?`/${encodeURIComponent(c)}`:""}`;
export async function createCampaignAction(brandId:string,form:FormData){try{const c=await createCampaign(brandId,{ideaId:String(form.get("ideaId")??""),name:String(form.get("name")??""),objective:String(form.get("objective")??"")});redirect(`${url(brandId,c.id)}?notice=${encodeURIComponent("Campaign created")}`)}catch(e){redirect(`${url(brandId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to create Campaign")}`)}}
export async function createAssetAction(brandId:string,campaignId:string,form:FormData){try{await createContentAsset(brandId,campaignId,{channel:String(form.get("channel")??"manual"),format:String(form.get("format")??"text"),audience:String(form.get("audience")??""),topic:String(form.get("topic")??""),hookType:String(form.get("hookType")??""),cta:String(form.get("cta")??""),content:String(form.get("content")??"")})}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to create content")}`)}redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent("Content Asset created")}`)}
export async function saveVersionAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number,form:FormData){try{await appendContentEdit(brandId,campaignId,assetId,{expectedVersion,content:String(form.get("content")??"")});redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent("New Content Version saved")}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to save version")}`)}}
export async function saveProductionAssetsAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number,form:FormData){try{const libraryAssetIds=form.getAll("libraryAssetId").map(value=>String(value));await saveContentVersionProductionAssets(brandId,campaignId,assetId,{expectedVersion,libraryAssetIds});redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent("Production assets saved as a new Content Version")}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to save production assets")}`)}}
export async function generateVersionAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number,action:string){try{await generateContentVersion(brandId,campaignId,assetId,{expectedVersion,action,brandContextVersion:`${brandId}@current`});redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent("AI-assisted version created")}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to generate version")}`)}}
export async function reviewContentAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number){try{await requestContentReview(brandId,campaignId,assetId,{expectedVersion,brandContextVersion:`${brandId}@current`,revisionCycle:0});redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent("Current version reviewed")}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to review version")}`)}}
export async function approveContentAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number,channel:"linkedin"|"instagram"|"facebook"|"manual",form:FormData){try{await approveContentVersionRequest(brandId,campaignId,assetId,{expectedVersion,destination:{channel,accountRef:String(form.get("accountRef")??"")}});redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent(`Version ${expectedVersion} approved`)}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to approve version")}`)}}
export async function scheduleContentAction(brandId:string,campaignId:string,assetId:string,form:FormData){try{const channelAccountId=String(form.get("channelAccountId")??"");const contentType=String(form.get("contentType")??"")as"text"|"image"|"video"|"carousel";if(form.get("publishMode")==="now"){await publishApprovedContentNow(brandId,campaignId,assetId,{channelAccountId,contentType});redirect(`/brands/${encodeURIComponent(brandId)}/calendar?notice=${encodeURIComponent("Approved version queued to publish now")}`)}await scheduleApprovedContent(brandId,campaignId,assetId,{channelAccountId,contentType,scheduledFor:String(form.get("scheduledForIso")??"")});redirect(`/brands/${encodeURIComponent(brandId)}/calendar?notice=${encodeURIComponent("Approved version scheduled")}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to schedule content")}`)}}
export async function distributeGroupAction(brandId:string,campaignId:string,assetId:string,expectedVersion:number,form:FormData){try{const[groupList,accounts]=await Promise.all([getChannelAccountGroups(brandId),getChannelAccounts(brandId)]);const group=groupList.find(item=>item.id===String(form.get("groupId")??""));if(!group)throw new Error("Account group not found");const memberIds=new Set(group.memberAccountIds);const destinations=accounts.filter(account=>memberIds.has(account.id)).map(account=>({assetId,expectedVersion,channelAccountId:account.id,contentType:String(form.get("contentType")??"text")as"text"|"image"|"video"|"carousel"|"reel"}));if(destinations.length!==group.memberAccountIds.length)throw new Error("Account group contains an unavailable destination");const result=await distributeCampaignRequest(brandId,campaignId,{scheduledFor:String(form.get("scheduledForIso")??""),destinations});const accepted=result.destinations.filter(item=>item.status==="scheduled"||item.status==="manual-required").length;const blocked=result.destinations.length-accepted;if(blocked){redirect(`${url(brandId,campaignId)}?notice=${encodeURIComponent(`${accepted} destinations accepted`)}&error=${encodeURIComponent(`${blocked} destinations need attention; review connection state, capability or approval policy`)}`)}redirect(`/brands/${encodeURIComponent(brandId)}/calendar?notice=${encodeURIComponent(`${accepted} group destinations accepted`)}`)}catch(e){redirect(`${url(brandId,campaignId)}?error=${encodeURIComponent(e instanceof Error?e.message:"Unable to distribute to account group")}`)}}
