import { NextRequest,NextResponse } from"next/server";
import{completeInstagramConnection}from"../../../../src/lib/instagram-api";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const code=request.nextUrl.searchParams.get("code"),state=request.nextUrl.searchParams.get("state"),providerError=request.nextUrl.searchParams.get("error");if(providerError||!code||!state)return NextResponse.redirect(new URL(`/?error=${encodeURIComponent(providerError?"Instagram connection was cancelled or denied":"Instagram callback was incomplete")}`,request.url));try{const result=await completeInstagramConnection(code,state);const base=`/brands/${encodeURIComponent(result.brandId)}/performance`;if(result.status==="selection-required")return NextResponse.redirect(new URL(`${base}?instagramIntent=${encodeURIComponent(result.intentId)}&notice=${encodeURIComponent("Choose the Instagram account Kairo should connect")}`,request.url));if(result.status==="no-eligible-account")return NextResponse.redirect(new URL(`${base}?error=${encodeURIComponent("No eligible Instagram Professional account was found")}`,request.url));return NextResponse.redirect(new URL(`${base}?notice=${encodeURIComponent("Instagram connected")}`,request.url))}catch(error){return NextResponse.redirect(new URL(`/?error=${encodeURIComponent(error instanceof Error?error.message:"Unable to complete Instagram connection")}`,request.url))}}
