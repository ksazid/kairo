import{describe,expect,it}from"vitest";import{DeterministicPublishingWorker,PublishingJobRunner,type PublishingExecutionStore,type PublishingJob, type ProviderPublishResult}from"./publishing";import{InstagramProfessionalAdapter,LinkedInOrganizationAdapter}from"./publishing-adapters";
const base:PublishingJob={commandId:"cmd",versionId:"v",idempotencyKey:"cmd:v:page",channel:"linkedin",accountRef:"urn:li:organization:123",credentialRef:"vault://li",contentType:"text",content:"Evidence-led post",mediaUrls:[]};const secrets={async resolve(r:string){expect(r).toMatch(/^vault:/);return"secret-token"}};
describe("VS-07 deterministic publishing adapters",()=>{it("publishes LinkedIn organization text through the official Posts endpoint",async()=>{let request:RequestInit|undefined;const adapter=new LinkedInOrganizationAdapter(secrets,"202607",async(input,init)=>{expect(String(input)).toBe("https://api.linkedin.com/rest/posts");request=init;return new Response(null,{status:201,headers:{"x-restli-id":"urn:li:share:1"}})});const result=await new DeterministicPublishingWorker([adapter]).execute(base);expect(result).toEqual({status:"published",externalPostId:"urn:li:share:1"});expect(request?.headers).toMatchObject({"linkedin-version":"202607","x-restli-protocol-version":"2.0.0"});expect(JSON.stringify(result)).not.toContain("secret-token")});
it("uses Instagram's container then publish flow for one public image",async()=>{const calls:string[]=[];const adapter=new InstagramProfessionalAdapter(secrets,"v24.0",async(input)=>{calls.push(String(input));return new Response(JSON.stringify({id:calls.length===1?"container-1":"media-1"}),{status:200,headers:{"content-type":"application/json"}})});const result=await adapter.publish({...base,channel:"instagram",accountRef:"123",credentialRef:"vault://ig",contentType:"image",mediaUrls:["https://cdn.example.com/image.jpg"]});expect(calls).toEqual(["https://graph.facebook.com/v24.0/123/media","https://graph.facebook.com/v24.0/123/media_publish"]);expect(result).toMatchObject({status:"published",externalPostId:"media-1",providerCorrelationId:"container-1"})});
it("fails closed to manual and preserves unknown network outcomes",async()=>{const adapter=new LinkedInOrganizationAdapter(secrets,"202607",async()=>{throw new Error("timeout")});expect(await new DeterministicPublishingWorker([]).execute(base)).toMatchObject({status:"manual-required"});expect(await adapter.publish(base)).toEqual({status:"unknown"});expect(new InstagramProfessionalAdapter(secrets,"v24.0").supports({...base,channel:"instagram",accountRef:"123",contentType:"image",mediaUrls:["http://127.0.0.1/private"]})).toBe(false)})});
describe("PublishingJobRunner",()=>{it("claims and settles one job without any model or agent port",async()=>{let settled:ProviderPublishResult|undefined;const store:PublishingExecutionStore={async claimNext(){return base},async settle(_,result){settled=result}};const ran=await new PublishingJobRunner(store,new DeterministicPublishingWorker([new LinkedInOrganizationAdapter(secrets,"202607",async()=>new Response(null,{status:201,headers:{"x-restli-id":"urn:li:share:2"}}))]),"worker-1",60,()=>new Date("2026-08-14T10:00:00Z")).runOnce();expect(ran).toBe(true);expect(settled).toMatchObject({status:"published"})})});
