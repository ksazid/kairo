# VS-73 context

Production inspection found the API running main SHA `23db5190612bf91e76e91cb608a1a8458ea5d462`, while Vercel production remained on web SHA `4edec13696f0066ad6b8f70218d3ca9119774d36`. This split deployment explains why later Research fixes could be present in the API but absent in the web deployment.

Current code also showed: the model gateway performed a single provider request with no 429 retry; research evidence adapters classified 429 but did not retry; the API had only initial Workspace+Brand creation and no endpoint to add another Brand to an existing Workspace; the Idea development service already skipped Research when persisted but the web detail page only rendered the development action before Research existed, preventing UI recovery when Strategist/Angle generation failed after Research persistence.

The owner explicitly granted full approval at 2026-08-21T05:27:00+02:00 to implement, verify, merge, release and production-enable/deploy the complete corrective scope without further approval prompts. Exact-SHA certification and deployment provenance must still be recorded truthfully after the candidate exists.

## Production smoke continuation — 2026-08-21

After the final VS-73 web/API release SHA `7900c1123553bccdb8a98565234e7752e0352854` was live, the authenticated smoke secret was supplied and the production harness created two isolated Brands in the existing Workspace: `Kairo Smoke Moto` and `Kairo Smoke SaaS`. All Brand-scoped read endpoints exercised by the harness returned 200 for both Brands, establishing the multi-Brand route and basic tenant-scoped reads in production.

The first full Research attempt then reproduced an upstream direct-model 429 after the bounded retry budget was exhausted. Kairo's existing governed Hermes runtime was enabled as the resilient primary runtime while preserving the direct model as fallback. The next Research attempt progressed through Hermes but failed closed during domain persistence with `A factual Claim requires evidence`. Root cause: Hermes had no explicit `research-dossier@1` or `strategist-angles@1` schema contract, while the TypeScript Researcher runtime validator accepted an empty `evidenceIds` array even though the domain correctly rejects factual/supporting Claims without evidence.

VS-73 testing was therefore reopened to add explicit Hermes Researcher/Strategist output contracts, require non-empty Research Claim evidence lineage at the runtime boundary, and cover the regression before repeating the production multi-Brand Research → Angles → Campaign smoke. No publishing authority, provider credentials, database schema, or external-post behavior was changed by this repair.

## Closeout decision — 2026-08-22

The Product Owner explicitly chose to defer the remaining deliberate concurrent-Research production verification so VS-73 can close against its original acceptance criteria. The concurrency scenario is preserved rather than rewritten away: production had previously exposed duplicate Research persistence risk, and the intended idempotent persistence repair remains part of the historical VS-73 implementation record. Only the dedicated fresh-Idea concurrent smoke/certification step is deferred.

Follow-up issue: #142 — `Follow-up: concurrent Research idempotency production verification`.

Issue #142 must verify single Research dossier persistence and idempotent candidate-Angle persistence under concurrent production requests. It is not a VS-73 certification gate. VS-73 closeout continues to require exact-SHA CI/Security/Product Intake evidence and truthful production verification for its accepted scope.
