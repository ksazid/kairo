# VS-71 Context — Research Start Flow Repair

## Trigger
During the VS-70 production smoke on TheDukeMan, the user opened an existing Idea (`External mods to improve performance`) and the product displayed `Ready to research` plus `Evidence gathering has not started.` with no action capable of progressing the Idea.

## Root cause
The repository already contains the VS-04 Researcher and Strategist orchestration and PostgreSQL persistence methods for Research Dossiers and candidate Angles. The authenticated API exposes Idea create/read and Angle select/edit only; the web layer mirrors that incomplete surface. The API test advances by calling the memory repository's `seedReadyBundle`, masking the missing production wiring.

## Corrective boundary
Restore the missing Idea → Research → Angles command path only. Reuse existing validated orchestration and persistence. Do not seed production data, fabricate evidence, bypass Research/Angle lineage, or weaken approval/publishing controls to unblock VS-70.

## Authorization
At 2026-08-20T10:31:00+02:00 the product owner explicitly instructed `Fix it` in response to the identified missing Research-start path. This context records that instruction as approval for the narrowly described corrective scope and implementation only. It does not grant certification, merge, release, deployment or production-enable authority.

## Design read
Page type: authenticated Idea decision workflow. User job: move a newly created Idea into evidence-backed Research. Density: low while Research is absent. Existing family: calm content-first Kairo workspace. The repair adds one obvious primary action inside the existing empty Research state and preserves mobile hierarchy, accessible form semantics and current tokens.

## Expected smoke after release
TheDukeMan → Ideas → `External mods to improve performance` → Start research → inspect evidence/uncertainty → select a LinkedIn text Angle → Campaign → Content Studio → review/approve → Publish now.
