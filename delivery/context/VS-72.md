# VS-72 Context — Research Evidence Relevance Repair

## Production observation

The TheDukeMan Idea `External mods to improve performance` successfully reached Research after VS-71 and its bodyless-POST hotfix. The resulting dossier exposed a factual-quality defect:

- off-topic Claim: external knowledge acquisition strategies improve firms' innovation performance;
- off-topic Claim: governmental monitoring improves state-owned-enterprise performance;
- only one Claim directly addressed aftermarket vehicle modifications, and it remained unresolved at 45% confidence.

## Root cause

`apps/api/src/server.ts` currently derives the research query by concatenating the Idea title and premise. Its `public-content-search` fallback is OpenAlex + Crossref, so ambiguous words such as `external` and `performance` can dominate scholarly retrieval.

`apps/worker/src/researcher.ts` de-duplicates/balances evidence but does not currently reject evidence for topical irrelevance before model synthesis.

## Approved correction

The user's `Fix` instruction at 2026-08-20T20:58:00+02:00 authorises scope + implementation for VS-72 only.

Implement deterministic Idea/evidence relevance gating and a focused scholarly query. Preserve the existing provider boundaries, source provenance, runtime budgets, structured validation, Truth/Claims controls and tenant isolation.

## Stop conditions

- Do not add a new crawler/search provider under this approval.
- Do not send Brand Brain/private Brand context into global scholarly queries.
- Do not alter publishing authority or migrations.
- If relevance cannot be established with enough evidence, fail the research attempt rather than persist a plausible-looking unrelated dossier.
- Certification, merge, release, production enablement and deployment require later approvals.