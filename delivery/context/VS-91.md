# VS-91 focused context — Frozen UI Shell and Home Fidelity

## Goal
Implement only the authenticated shell plus frozen Home fidelity batch. Do not expand to other frozen pages in this slice.

## Read first

- `AGENTS.md`
- `product/PRD.md`
- `product/TRD.md`
- `product/DESIGN.md`
- `product/DESIGN-APPROVALS.md`
- `product/APPROVED-UI-PAGES-CONTEXT-2026-08-25.md`
- `product/approved-ui/README.md`
- `docs/plans/KAIRO-UI-TRACEABILITY-2026-08-24.md`
- `docs/plans/KAIRO-APPROVED-UI-FIDELITY-IMPLEMENTATION-2026-08-25.md`
- `docs/slices/VS-91.md`

## Frozen visual target

Use the Home screenshot/fingerprint recorded in `product/APPROVED-UI-PAGES-CONTEXT-2026-08-25.md` and the indexed board under `product/approved-ui/` as the exact visual target. Do not regenerate or reinterpret it.

## Authority deltas to apply

- User-facing navigation label is `Insights`.
- Final frozen Home body is the approved Home master reference; do not re-add older `Up Next` / `Continue` sections if absent from that frozen reference.
- Brand context appears once in the shared shell, not repeated as a Home hero card.
- Internal Research/Hunter/Angles/Campaign/Critic/Claims/provider/render/version language is hidden from normal creator navigation/body.
- Runtime truth wins: no fabricated analytics/readiness/connection/publishing data.
- Integrations is outside this slice and outside the frozen implementation sequence unless explicitly reopened by the Product Owner.

## Allowed runtime paths

- `apps/web/**`
- `packages/design-tokens/**`
- `tests/**`

Governance/evidence paths:

- `docs/slices/VS-91.md`
- `docs/plans/**`
- `delivery/**`
- `.engineering/STATE.json`
- `scripts/**`

## Protected/out-of-scope runtime paths

- `apps/api/**`
- `apps/worker/**`
- `apps/api/migrations/**`
- `infrastructure/**`
- `.github/workflows/release.yml`

Do not touch protected/out-of-scope runtime paths to make UI work easier.

## Installed design/engineering skills

Use in sequence and subordinate to repository authority:

1. UI UX Pro Max
2. Impeccable
3. Emil Design Engineering (only if motion clarifies state/feedback)
4. Ponytail
5. UI Review
6. Superpowers implementation/review workflow

`design-taste-frontend` is not the primary workflow skill for this authenticated product surface and may not introduce a new visual language.

## Verification

For every implementation step:

1. inspect the incumbent component/root cause;
2. prefer shared tokens/components;
3. run smallest relevant tests;
4. run runtime typecheck/tests/build;
5. render desktop;
6. render mobile;
7. compare with frozen reference;
8. exercise truthful applicable states;
9. run accessibility/responsive/UI Review;
10. fix Blocker/Major discrepancies once;
11. perform one confirmation render;
12. run governance/preflight before certification request.

No merge, release or production-enable authority is granted by VS-91 implementation approval.

## Implementation evidence — 2026-08-25

Implemented on `ui/approved-fidelity-implementation-2026-08-25`:

- normalized the authenticated shell to the five frozen destinations `Home / Content / Calendar / Insights / Brand`;
- removed persistent Product Guide and legacy breadcrumb chrome from the frozen shell;
- kept Brand identity in shell context rather than repeating it as the Home page identity;
- replaced the older six-section Home with the final frozen four-part master: `Needs Attention / My Idea / For You / What's Working`;
- made Needs Attention singular and dominant;
- added URL / Photo / Video / + Media controls and an explicit `Get recommendations` step to My Idea;
- implemented horizontal For You cards with truthful unavailable thumbnail/save states and real opportunity-derived Impact/Fit signals;
- implemented the exact Reach / Saves / Shares / Engagement rate slots with unavailable states rather than fabricated values or trends;
- removed Home links/copy that expose legacy Discover/Hunter/Campaign/Idea/learning machinery;
- added deterministic frozen-shell/Home contract tests;
- saved unavailable runtime behavior in `docs/plans/KAIRO-UI-FUNCTIONALITY-GAPS-2026-08-25.md`.

Truthfulness review also removed generic `engagement` as a source for the `Engagement rate` card so a count cannot be mislabeled as a rate.

Automated evidence on runtime candidate `0e6d0802797c89b794bbe24aa56adb42db2f5f5a`:

- Product Intake run `32852174639` — passed;
- Security baseline run `32852174701` — passed;
- CI run `32852174620` — passed, including preflight and runtime verification/build;
- branch compare against `main` at review time: ahead, `behind_by: 0`;
- PR #197 review threads/comments: none open.

### Rendered-fidelity evidence status

Not yet claimed. Repository `vercel.json` has `git.deploymentEnabled: false`, so this branch has no Git-generated preview deployment. The connected CI artifact contains only the PES dashboard, not the built web runtime. Desktop/mobile screenshot comparison against the frozen reference therefore remains the final testing evidence required before certification/merge-readiness. The deployment gate has not been weakened or bypassed.

## Preserved prior observation

VS-90 remains released/observed with authenticated production observation issue #192 open. Its outstanding observation is preserved and must not be represented as validated merely to activate VS-91.
