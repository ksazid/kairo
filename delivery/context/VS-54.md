# VS-54 focused context — Video Creation / Editing Tooling

## Authority
- `AGENTS.md`
- `product/PRD.md`: FR-11, FR-12, FR-13, FR-15
- `product/TRD.md`: modular monolith, private generated media, provider-neutral ports, deterministic publishing, Brand isolation
- `product/DESIGN.md`: calm editor-first Content Studio, progressive disclosure, human control
- `docs/slices/VS-18.md`
- `docs/slices/VS-20.md`
- `docs/slices/VS-54.md`

## Approved outcome
Add a human-editable Kairo Video Project/timeline layer over the existing Reel contracts and render pipeline. The project must compile back through `validateReelPlan`; rendering/export must reuse the VS-18/VS-20 infrastructure.

## Existing contracts to reuse
- `packages/domain/src/creative-formats.ts`: `ReelPlan`, `ReelScene`, `validateReelPlan`
- `apps/worker/src/creative-renderer.ts`: deterministic Reel storyboard/render manifest
- `apps/worker/src/reel-encoder.ts`: bounded provider-neutral Reel MP4 encoder boundary
- `apps/worker/src/publishable-media.ts`: private publishable-media preparation
- `packages/domain/src/campaign.ts`: immutable `ContentVersion` lineage
- `packages/domain/src/review.ts`: exact-version review/approval authority

## Parallel isolation
The Media Transformation Pipeline is owned by another active Kairo chat. Do not implement or modify generic resize/crop/transcode/compress/thumbnail/platform-preparation capability here. Do not overwrite `delivery/current-slice.json` while that parallel work is active; reconcile it only before certification after re-reading latest `main`.

## Allowed initial paths
- `packages/domain/src/video-project.ts`
- `packages/domain/src/video-project.test.ts`
- `packages/domain/package.json`
- bounded VS-54-only web components/routes under the existing Campaign/Content Studio surface
- bounded review/display adapters required only to present a Video Project as visible content
- `docs/slices/VS-54.md`
- `docs/plans/VS-54-*.md`
- `delivery/context/VS-54.md`

## Protected / out-of-scope paths
- `.github/workflows/release.yml`
- `infrastructure/**`
- `apps/api/migrations/**`
- provider/OAuth configuration
- new media-transformation files owned by the parallel slice
- generic media transformation/encoding infrastructure unless a regression fix is proven necessary
- `delivery/current-slice.json` until parallel-slice reconciliation

## Human gates
Scope + bounded implementation: approved 2026-08-18T00:37:00+02:00. Certification, merge, release, deployment and production enablement remain pending and require their normal exact-SHA/human gates.
