# VS-83 — Production Object Storage and Live Carousel Verification

## Status
The exact approved SHA `e794cf7207be793db888990f0029f4155d6f193a` is physically live on Render, while VS-83 remains **release-pending** in governance until the required authenticated provider-backed carousel verification passes.

Certification, release and production enablement were explicitly approved by the Product Owner for that exact SHA on 2026-08-23. Render deployment `dep-da5ea964a6sc73fa0fmg` is live on the approved SHA and the Instagram publisher started successfully. REL-005 remains `deploying` because Kairo's governance model requires passed production verification before a release may be marked `released`.

Release completion remains open until the exact recovered carousel is replayed through an authenticated Kairo user session and reaches provider-backed Published → Calendar → Results evidence. Connected infrastructure tooling intentionally does not expose the user's OIDC bearer token, so that authenticated action must not be bypassed.

## Outcome
Kairo renders approved Instagram carousels into private production object storage, exposes only bounded signed delivery URLs, and verifies that Meta publishes the exact approved asset version.

## Renderer incident and release evidence
The production smoke before PR #167 recovered this exact lineage:

- Brand: `f00f0ec6-53bb-40da-ac20-f4c2ecb381d7`
- Campaign: `e5e2b57b-3b78-45a8-a4f1-a95ddc686198`
- Asset: `964bf2ac-a190-40c1-b886-af17fd265640`
- Carousel Project: `carousel-acd92c95-5308-4b81-9bf5-92d3c8b17809`

Bootstrap returned `201`, then the old fixed-scale renderer failed the render request with `Creative text does not fit the configured render area` before quality evaluation.

PR #167 replaced that boundary with deterministic adaptive text fitting, retained fail-closed quality enforcement, and added regressions covering live-style copy plus body/CTA copy near the Carousel Project contract maxima. Exact PR head `3be431034eb58bd53885d2bf9a68f391116d98eb` passed CI #1083, Security #895, governance/preflight, workspace typecheck/tests/builds and Vercel checks. Squash merge SHA `e794cf7207be793db888990f0029f4155d6f193a` has the identical Git tree `9eec892d5f3a1790e0f8700c4a54f84ff509ebb1`.

## In scope — combined batch
1. Add a replaceable S3-compatible private creative object adapter.
2. Upload rendered slides, thumbnails and manifests with immutable content-addressed keys.
3. Add deterministic SigV4 temporary delivery for private objects.
4. Add an authenticated server-side carousel render action.
5. Run the existing measured quality checks against the actual rendered PNGs.
6. Persist the render, slide, thumbnail and manifest lineage as one ready asset version.
7. Fail closed on unsafe storage configuration, object hashes, dimensions or blocking quality findings.
8. Make Review and publishing consume the same signed private objects.
9. Extend production configuration and smoke verification for exact approved carousel media.
10. Complete one provider-backed carousel publish and verify Meta media ID, URL, Calendar and Results.

## Acceptance criteria
- [x] Storage credentials never enter URLs, logs, persisted render records or client responses.
- [x] Private objects use immutable Brand-scoped keys and server-verified SHA-256 hashes.
- [x] Signed delivery is HTTPS, host-bound, time-bounded and read-only.
- [x] Rendering produces 1080x1350 PNG slides and a deterministic thumbnail.
- [x] Blocking quality findings prevent a ready render from being approved.
- [x] Render records bind the manifest, every slide and thumbnail to the exact project revision.
- [x] Review previews and Meta publishing resolve the same approved object keys.
- [x] Tenant isolation and optimistic concurrency remain enforced.
- [ ] A provider-backed carousel reaches Published with Meta result evidence.
- [x] Runtime, build, security and governance checks pass before exact-SHA merge approval.
- [x] Exact-SHA certification, release and production-enable approvals are recorded.
- [x] Exact approved SHA is live on Render with rollback readiness.

## Final release verification action
Replay Carousel Project `carousel-acd92c95-5308-4b81-9bf5-92d3c8b17809` under the existing authenticated Brand scope, then verify:

1. render succeeds without the former text-layout exception;
2. measured quality has zero blocking findings before approval;
3. the exact rendered version is approved;
4. Instagram publishes the approved private media through the existing Meta capability gate;
5. provider media ID/published URL are persisted;
6. Calendar shows Published;
7. Results exposes provider-backed observations for the same Brand/asset lineage.

Only after that evidence passes may REL-005 be marked `released` and VS-83 progress through `released` → `observed` → `validated`.

## Allowed paths
- `apps/api/**`
- `apps/worker/**`
- `packages/domain/**`
- `package-lock.json`
- `product/**`
- `planning/**`
- `docs/slices/VS-83.md`
- `docs/plans/VS-83-implementation.md`
- `delivery/**`
- `scripts/**`
- `README.md`
- `.engineering/STATE.json`

## Protected paths
- `.github/workflows/release.yml`
- `infrastructure/**`

## Explicit exclusions
- public buckets or permanent public media URLs;
- client-side storage credentials or direct unverified uploads;
- publishing a render other than the exact human-approved version;
- bypassing Kairo authentication to complete production verification;
- release, deployment or production enablement without exact-SHA approval.
