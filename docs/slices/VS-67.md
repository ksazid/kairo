# VS-67 — Run C Qualification Authorization

## Purpose

Authorize exactly one fresh Marketing Lab paired qualification evidence run after VS-66 introduced the lane-symmetric `closed-world-claims-v1` generation contract.

## Approved run

- Run ID: `vs23-qualification-20260820-c`
- Evidence type: four paired Kairo Native vs Corey motorcycle-carousel cases
- Runtime: existing DirectModel route only
- Existing benchmark fixtures, Corey source pin, provider/model, pricing, pacing and thresholds remain unchanged
- Run B remains immutable historical evidence

## Implementation

Rotate only the exact startup and authorization allowlists from Run B to Run C:

- `scripts/start-api.mjs`
- `scripts/authorize-marketing-shadow-evidence.mjs`

The underlying evidence store and runner remain unchanged and continue to require:

- exact run ID;
- exact lowercase 40-character `KAIRO_RELEASE_SHA`;
- `RENDER_GIT_COMMIT == KAIRO_RELEASE_SHA` on Render;
- one durable DB authorization consumed transactionally before any model work;
- one durable run row keyed by run ID;
- exactly four paired cases;
- stable DirectModel provider/model/pricing route;
- measured cost and latency metadata.

## Exclusions

VS-67 does not modify benchmark fixtures, generation prompts, Truth policy, evaluator rubric, model-output schemas, migrations, provider credentials, pricing, pacing, Hermes, human scoring, edit-distance evidence, deterministic advancement logic, publishing, or VS-23/VS-24 activation.

## Release protocol

1. Certify the exact allowlist implementation head through Product Intake, Security and full CI.
2. Bind release and production-enable approvals to that exact implementation head.
3. Merge the final governance-only certification candidate.
4. Deploy the resulting exact `main` SHA with all Marketing Lab execution flags dormant.
5. Enable exactly `KAIRO_MARKETING_SHADOW_EVIDENCE_RUN=1`, `KAIRO_MARKETING_SHADOW_EVIDENCE_RUN_ID=vs23-qualification-20260820-c`, and `KAIRO_STARTUP_MARKETING_SHADOW_AUTHORIZATION=vs23-qualification-20260820-c` with `KAIRO_RELEASE_SHA` equal to the exact deployed SHA.
6. Require the authorization-staged log before evidence execution can be considered authorized.
7. Require terminal durable `KAIRO_MARKETING_SHADOW_EVIDENCE_COMPLETE` with pair count 4.
8. Immediately clear authorization/execution flags and verify the cleanup deploy is live.

No evidence may be fabricated or backfilled if execution fails.
