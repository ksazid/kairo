# VS-75 — Structured Carousel Foundation

## Status
Approved for runtime implementation by Sazid Khan on 2026-08-22 as Batch 2 of the approved Instagram completion work.

## Requirements
- FR-08 — Content development
- FR-11 — Content Studio
- FR-13 — Approval workflow
- FR-15 — Publishing

## Outcome
Kairo produces Brand-aware, Instagram-compatible structured carousel projects through a replaceable rendering seam and reports deterministic visual-quality findings before later review, approval and publishing integration.

## In scope — Batch 2 tasks 11–20
1. Add CarouselProject structures: AIDA, PAS, listicle, case study, story and comparison.
2. Add stable slide IDs and semantic slide roles.
3. Compile CarouselProject into the existing Claim-linked CarouselPlan contract.
4. Enforce Instagram-compatible 2–10 slide production bounds.
5. Add Idea → Angle → content type → Carousel/Reel structured development output.
6. Add a replaceable CarouselRendererPort/engine seam; no engine is coupled to MCP or domain.
7. Add bounded Brand visual-theme inputs for colors, font labels and logo placement metadata.
8. Make the Instagram carousel preset 1080×1350 (4:5).
9. Add deterministic quality findings for overflow, alignment, contrast, whitespace, logo placement, copy density, CTA position and Brand consistency.
10. Add deterministic contract, compilation, renderer and quality-rule tests.

## Acceptance criteria
- [x] All six structures validate with explicit semantic slide roles and stable unique slide IDs.
- [x] Claim lineage is preserved when a project compiles to CarouselPlan.
- [x] Instagram production rejects projects outside 2–10 slides.
- [x] Structured development output preserves Idea, Angle and content-type lineage.
- [x] Renderer implementation remains replaceable behind a provider-neutral port.
- [x] Instagram preset emits 1080×1350 PNGs deterministically.
- [x] Brand visual inputs are bounded and cannot cause arbitrary asset/network access.
- [x] Every named quality category produces deterministic severity and evidence.
- [x] Existing carousel/Reel contracts remain backward compatible.
- [x] Focused tests, full runtime verification and governance preflight pass.

## Allowed paths
- `packages/contracts/**`
- `packages/domain/**`
- `apps/worker/**`
- `docs/slices/VS-75.md`
- `docs/plans/VS-75-implementation.md`
- `delivery/**`
- `.engineering/STATE.json`

## Explicit exclusions
- BearCarousel source import or dependency until license/security review is recorded;
- database migration or production API/UI wiring;
- image upload, arbitrary remote image fetching or custom font execution;
- visual editor, per-slide regeneration, approval binding or publishing changes;
- MCP, merge, release, deployment or production enablement.
