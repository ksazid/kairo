# VS-68 — Run D Qualification Hardening and Authorization

## Purpose

Run C (`vs23-qualification-20260820-c`) is immutable terminal evidence with `marketing_shadow_execution_error`. VS-68 hardens the existing paired qualification path before one fresh Run D attempt.

## Scope

- preserve Run C unchanged;
- eliminate the known gap where Groq strict JSON Schema could accept Claim IDs outside the supplied benchmark lineage and Kairo rejected them only after generation;
- emit bounded non-sensitive failure provenance containing case, lane, stage and stable error code;
- retain the exact four motorcycle/carousel fixtures, Corey pin, DirectModel provider/model route, pricing, 65-second pacing, cost/latency limits and Truth policy;
- rotate the one-shot authorization allowlist to `vs23-qualification-20260820-d`;
- execute Run D only after exact-head certification and a dormant exact-SHA production deployment.

## Failure provenance

Failure codes must be safe identifiers only. They may identify the approved case (`mc01`..`mc04`), lane (`native` or `corey`), stage (`execute`, `fingerprint`, `metadata`, `route`) and a stable error code. They must not contain prompts, model output, provider response bodies, credentials, private Brand data or arbitrary exception messages.

## Guardrails

- no in-lane retries;
- no benchmark fixture edits;
- no human score or edit-distance fabrication;
- no qualification threshold changes;
- no provider/model/key/pricing mutation;
- no Hermes reactivation;
- no publishing or VS-23/VS-24 activation.

## Run D

Reserved fresh run ID: `vs23-qualification-20260820-d`.

A terminal Run D result is immutable. Whether it completes or fails, all Marketing Lab execution/startup triggers must be cleared immediately afterward.
